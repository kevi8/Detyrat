@{
    ViewData["Title"] = "Home Page";
}
    <h1>JumpStart your project today!</h1>
    <div class="gap-5 d-flex">
        <div>
            @await Html.PartialAsync("_Register")
        </div>
        <div>
            @await Html.PartialAsync("_Login")
        </div>
    </div>